{
  "name": "tennu-correction-db",
  "version": "1.0.2",
  "description": "Middleware for tennu-correction, hooks into tennu-dbcore for messages stored by tennu-dblogger.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tennu/tennu-correction-db"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {}
}
